
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      # persist the sqlite database file
      - sqlite_data:/app/data
    restart: unless-stopped

volumes:
  sqlite_data:
